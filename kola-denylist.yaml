# This file documents currently known-to-fail kola tests. It is consumed by
# coreos-assembler to automatically skip some tests. For more information,
# see: https://github.com/coreos/coreos-assembler/pull/866.

# The latest dracut upgrade caused the tests to fail
- pattern: coreos.unique.boot.*
  snooze: 2026-02-20
  arches:
    - ppc64le
  tracker: https://github.com/coreos/rhel-coreos-config/issues/172
  osversion:
    - rhel-9.8
# The latest dracut upgrade caused the test to fail
- pattern: coreos.ignition.failure
  snooze: 2026-02-20
  arches:
    - ppc64le
  tracker: https://github.com/coreos/rhel-coreos-config/issues/172
  osversion:
    - rhel-9.8

# Chronyd got updated to use a new device but the
# kernel patch exposing that device haven't landed
# yet.
- pattern: ext.config.shared.rpm-ostree.kernel-replace
  snooze: 2026-03-10
  arches:
    - s390x
  tracker: https://github.com/coreos/rhel-coreos-config/issues/150
  osversion:
    - rhel-10.2

# This test is failing only in prow, so it's skipped by prow
# but not denylisted here so it can run on the rhcos pipeline
#- pattern: iso-offline-install-iscsi.ibft.bios
#  tracker: https://github.com/openshift/os/issues/1492

# This test is failing only in prow, so it's skipped by prow
# but not denylisted here so it can run on the rhcos pipeline
#- pattern: pxe-offline-install.rootfs-appended.bios
#  tracker: https://github.com/openshift/os/issues/1768

- pattern: ostree.sync
  tracker: https://github.com/openshift/os/issues/1751
  osversion:
    - centos-10
    - rhel-10.2
